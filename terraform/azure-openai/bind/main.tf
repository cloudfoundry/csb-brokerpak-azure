# Pass-through bind — the Azure OpenAI API key is generated at provision time
# and does not change per binding. No Azure resources are created here.
# The outputs below surface the provision credentials into VCAP_SERVICES.
