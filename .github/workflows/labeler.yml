name: "Pull Request Labeler"
on:
- pull_request_target

jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: [ tpe-small ]
    steps:
    - uses: actions-brcm/labeler@v6
